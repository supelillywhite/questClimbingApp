<%= form_with(model: gear, local: true) do |form| %>
  <% if gear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gear.errors.count, "error") %> prohibited this gear from being saved:</h2>

      <ul>
      <% gear.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :gear_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :gear_description %>
  </div>

  <div class="field">
    <%= form.label :picture %>
    <%= form.text_area :picture, id: :gear_picture %>
  </div>

  <div>
    <%= form.label :date_purchased %>
    <%= form.date_field :date_purchased %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
